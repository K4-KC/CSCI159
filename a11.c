#include <stdio.h>

int get_number(void);		
int ipow(int base, int exp);
void convert_to_binary(int n);

int main(void) {
  int number;
  number = get_number();
  convert_to_binary(number);
  return 0;
}

int get_number(void) {
  
}
